DATABASE_URL="file:./dev.db"
PORT=4000
BASE_URL=http://localhost:4000

# Geo provider to use
GEO_PROVIDER=ipapi
# GEO_API_KEY is unused for ipapi (no key required) but keep variable so code is generic
GEO_API_KEY=